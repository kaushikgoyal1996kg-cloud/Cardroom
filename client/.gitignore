node_modules/
dist/
*.tsbuildinfo
*.log

# Environment files — real secrets (e.g. VITE_TURN_CREDENTIAL) live in
# .env.local. Never commit it. Templates stay tracked.
.env
.env.local
.env.*.local
!.env.example
!.env.local.example
